<% if @bookmarks.empty? %>

<%# what is returned if there no results %>
<div class="no-search-results">
    <div class="box-center">
      <div grid-item>
        <%= image_tag "search/sorrymsg.png", class: "index-error" %>
      </div>

    </div>
</div>

<% else %>
<div class="wrapper">
<button class="search-filter"> Filters ☰ </button>
</div>



<div class="renbo-container">
<div class="home-heading">
  <div>
    <h3 id="index-heading"> My Bookmarks </h3>
  </div>
</div>
  <div class="event-card-grid">
    <% @events.each do |event| %>
      <%= render "shared/card", event: event %>
    <% end %>
  </div>
</div>
<% end %>
